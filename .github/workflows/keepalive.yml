name: Keep Render Service Alive

on:
  push:
    branches:
      - main
  workflow_dispatch:        
  schedule:
    - cron: "*/15 * * * *"  

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -s ${{ secrets.API_URL }}/health || true